name: Monitor CRAC

on:
  schedule:
    - cron: '0 * * * *'  # Run every hour

jobs:
  run_script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run check_crac_qinghai.py
        run: python check_crac_qinghai.py